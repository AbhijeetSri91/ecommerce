import { WhiteSpaceValidators } from './white-space-validators';

describe('WhiteSpaceValidators', () => {
  it('should create an instance', () => {
    expect(new WhiteSpaceValidators()).toBeTruthy();
  });
});
